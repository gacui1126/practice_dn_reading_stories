<div class="row">
      <% @stories.each do |i| %>      
          <div class="content-left col-xs-3 col-sm-3 col-md-3 col-lg-3">
            <div class = "card" >
                  <img  class="image_size" src="<%= i.img%>" alt=<%= i.story_name %> >
                  <div class="caption">
                      <a href="/#" title="<%= i.story_name %>"><%= i.story_name %></a>
                  </div>
                  <p>Author: <%= i.author %></p>
            </div>
          </div>      
      <% end %>
</div> 